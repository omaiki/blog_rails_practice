<h1>Listing articles</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
  </tr>
</table>

<%= @articles.each do |article| %>
<tr>
  <td><%= article.title %></td>
  <td><%= article.text %></td>
  <td><%= link_to article.title, article %></td>
</tr>
<% end %>